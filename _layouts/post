---
layout: default
---
<article>
  <header>
    <h2>{{ page.title }}</h2>
    <p>
      {{ page.date | date_to_string }}
    </p>
  </header>
  {{ content }}
  {% if page.file %}
    <audio src="{{ page.file }}" type="audio/mpeg"></audio>
  {% endif %}
</article>
